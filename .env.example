# GitHub Issue Digest Bot - Environment Variables Example
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos), public_repo (for public repos)
GH_TOKEN=ghp_your_github_token_here

# SMTP Configuration for sending emails
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Email Configuration
EMAIL_FROM=GitHub Digest <your-email@gmail.com>
EMAIL_TO=your-email@gmail.com

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Search queries for issues (comma-separated)
# Default: is:issue is:open no:assignee label:"good first issue"
SEARCH_QUERIES=repo:your-org/your-repo is:issue is:open

# Search queries for pull requests (comma-separated)
# Default: is:pr is:open review-requested:@me
PR_QUERIES=repo:your-org/your-repo is:pr is:open

# Priority labels to include (comma-separated)
# Only issues/PRs with at least one of these labels will be included
PRIORITY_LABELS=priority:p0,priority:p1,enhancement,feature,performance

# Labels to exclude (comma-separated)
# Issues/PRs with any of these labels will be excluded
EXCLUDE_LABELS=low-priority,wontfix,duplicate

# Assignees to exclude (comma-separated)
# Issues/PRs assigned to these users will be excluded
EXCLUDE_ASSIGNEES=bot,automation

# Maximum number of issues to include
MAX_ISSUES=10

# Maximum number of PRs to include
MAX_PRS=10
